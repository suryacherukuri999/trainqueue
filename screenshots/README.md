# screenshots

Demo media for the project (referenced from the root README).

Record a 30–60s gif of the end-to-end flow and drop it here as `demo.gif`:

1. submit a job in the console
2. open its detail page — live logs + the loss curve animating
3. let it finish, then search its logs and download the artifact

Then embed it at the top of the root README:

```md
![demo](screenshots/demo.gif)
```
