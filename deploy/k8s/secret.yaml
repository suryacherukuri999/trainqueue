# Shared API key. Replace the value before deploying (and build the console image
# with a matching VITE_API_KEY).
apiVersion: v1
kind: Secret
metadata:
  name: trainqueue
type: Opaque
stringData:
  api-key: change-me-in-production
